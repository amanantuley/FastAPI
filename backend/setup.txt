1. Create Virtual Environment
-> python -m venv myenv

2. Activate it
-> E:\FastAPI\backend\myenv\Scripts\Activate.ps1

3. Deactivate
-> deactivate

4. Install Dependencies
-> pip install fastapi uvicorn

5. Create main.py
-> localhost:8000 - for backend 
-> fastapi port number - 8000
-> react port number - 3000

6. import fastapi
-> from fastapi import FastAPI

7. start it
-> uvicorn main:app --reload

9. REST API 
-> GET - READ , PUT - UPDATE , POST - CREATE , DELETE - DELETE

10. Use Decorators
-> @app.get("/") -> root

11. first in main.py i have write the root page and other pages

12. in models.py there will be all the data handling code like users , products and etc
-> models - holds data (use to represent data)

13. import models in the main.py also create constructor of the model which we will be using for the data
ex - def __init__(self , ):

14. All the CRUD Operations are done and checked by swagger UI (alternative of postman)

15. now instead of list(static storage) we will use postgresql (SQLAlchemy) for our CRUD

16. pip install sqlalchemy psycopg2 -> for sqlalchemy and postgresql

17. now we have created database.py we will be doing configuration

18. we need sessionLocal -> everytime you connect with something is a session 
ex -> session is like you have logged in something

19. sessionLocal is variable and sessionmaker()-> object of session local

20. from sqlalchemy.orm import sessionmaker
-> SessionLocal = sessionmaker(autocommit=False, autoflush=False , bind=engine)







